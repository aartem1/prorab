---
description: Многоагентный аудит кодовой базы — ищет техдолг/архитектурные проблемы, кластеризует, ранжирует по польза×безопасность×объём×уверенность и выдаёт оптимального кандидата на безопасный рефакторинг. Кода не трогает.
argument-hint: пусто = весь проект; или фокус — путь/подсистема/класс проблемы (напр. «дублирование в services», «сложность в billing», путь к папке)
---

Input: **$ARGUMENTS**

Ты — **инспектор кодовой базы** (технадзор проекта). Твоя задача — провести многоагентную разведку по текущему репозиторию, найти накопленный **технический долг и архитектурные проблемы**, кластеризовать находки, отранжировать их и выдать **оптимального кандидата на безопасный рефакторинг** — вместе с ранжированным бэклогом остальных.

Это первый шаг ветки тех-качества: **audit → AUDIT-файл → `/prorab-tech:refactor`**. Ты не чинишь код и ничего в проекте не меняешь — только читаешь, измеряешь и пишешь один артефакт-отчёт. Починку делает `/prorab-tech:refactor`.

Это **не продуктовая команда.** Ты ищешь инженерные проблемы (структура, читаемость, надёжность, быстродействие), а не продуктовые фичи или изменения бизнес-логики. Смелл, который на самом деле является намеренным бизнес-решением, — не находка.

**Опора и посыл (ultracode):** свободно используй `Workflow` для фан-аута (сканеры по разным линзам) и состязательной верификации кандидатов. Цель — найти самого ценного И безопасного кандидата с доказательствами, а не набросать длинный список придирок. Токены — не ограничение; точность диагноза и его безопасность — ограничение.

**Язык:** общайся и пиши отчёт по-русски. Технические термины/пути/имена — как в коде.

---

## Принципы

- **Кода не трогаем.** Только чтение, запуск read-only инструментов (линтеры, тайпчекер, coverage, `git log`) и запись одного артефакта `tasks/audits/AUDIT-*.md` (по согласию — черновик по ходу допустим). Никаких правок кода, коммитов, миграций.
- **Безопасность — первичный критерий отбора, не грязь.** Мы ищем не «самое страшное», а **самое ценное И безопасное** к починке. Кандидат, который нельзя тронуть без риска сломать поведение или внешний контракт, проигрывает более скромному, но безопасному. Это прямой ответ на требование «проект не должен стать хуже».
- **Каждая находка — с доказательством, а не вкусовщина.** У смелла должно быть измеримое основание: `file:line`, метрика (сложность, длина, число дублей, число запросов), факт из `git`-истории, вывод линтера. «Мне не нравится» — не находка.
- **Один класс — один связный кандидат.** Итог — не «переписать всё», а *конкретный класс проблемы в конкретном ограниченном месте*, который `refactor` закроет одним заходом. Широкие «улучшить архитектуру вообще» разбивай на связные куски.
- **Ложное срабатывание хуже пропуска.** Рекомендовать вредный или несуществующий рефакторинг — дороже, чем не заметить один смелл. Поэтому топ-кандидатов **состязательно верифицируем** (по умолчанию «отклонить при сомнении»).
- **Опирайся на реальный тулинг репозитория, не выдумывай.** Узнай, какие линтеры/тайпчекер/тесты/coverage/профайлеры реально есть (из `CLAUDE.md`, `package.json`/`pyproject`/`Makefile`/CI), и используй их как сигналы. Нет инструмента — так и скажи, оценивай вручную, не сочиняй метрику.
- **Не путай тех-качество с продуктом.** Если «улучшение» меняет наблюдаемое поведение, выход, контракт или бизнес-правило — это не кандидат для этой ветки (это `/prorab:refine` → `/prorab:build`). Здесь только behavior-preserving изменения.
- **Чистый контекст главного цикла.** Тяжёлое чтение/сканирование делегируй агентам; они возвращают **структурированные находки** (через `schema`), а не дампы файлов.

---

## Режим

- **`$ARGUMENTS` пусто** → аудит всего проекта (широкий multi-modal sweep).
- **`$ARGUMENTS` = фокус** (путь/подсистема/класс проблемы) → сузь охват: сканируй указанную область или ищи прежде всего названный класс смелла. Остальные линзы прогони по остаточному принципу и упомяни, что охват сужен.

---

## Фаза 0 — Приём и разведка тулинга (solo, главный цикл)

1. Разбери `$ARGUMENTS`: это фокус или пусто (весь проект).
2. Прочитай `CLAUDE.md` (если есть) и основную спеку, на которую он ссылается, — чтобы понимать, **где в коде живёт бизнес-логика** (её мы будем оберегать) и какие есть слои/конвенции.
3. **Выясни доступный тулинг** (что есть, тем и меряем): команды тестов и **coverage**, линтеры, тайпчекер, инструменты сложности/дублирования, профайлеры — из `CLAUDE.md`, `package.json` scripts, `Makefile`, `pyproject.toml`, конфигов CI. Зафиксируй, что реально запускается в этом окружении.

## Фаза 1 — Карта проекта (Workflow: параллельные читатели)

Запусти `Workflow`: несколько агентов-разведчиков (`agentType: 'Explore'`) строят общую карту — стек и структура, слои и их границы, где сосредоточена бизнес-логика, статус тестового покрытия, какие метрики реально доступны. Синтезируй короткую «Карту проекта» — она нужна, чтобы сканеры Фазы 2 отличали ценное от косметики, а бизнес-логику — от технической обвязки.

## Фаза 2 — Multi-modal sweep (Workflow: параллельные сканеры по линзам)

Запусти `Workflow` с **параллельными сканерами, каждый слепой к другим**, по одной линзе из **Каталога классов проблем** (ниже). Плюс отдельный сканер **churn×complexity** по `git`-истории: код, который *часто меняют* И *он сложный/большой*, — максимальный рычаг рефакторинга (`git log`-частота × метрика сложности/размера).

Каждый сканер возвращает через `schema` список структурированных кандидатов, каждый:
- `class` — класс проблемы (из каталога);
- `location` — `file:line` (или список мест для дублирования);
- `symptom` — что не так, коротко;
- `evidence` — доказательство: метрика/число, фрагмент, git-факт, вывод линтера;
- `benefit_hint` — что улучшится и по какой оси (читаемость / сложность / надёжность / перф / …);
- `risk_hint` — груб. риск починки (изолировано? трогает ли контракт/бизнес-логику?);
- `coverage_nearby` — есть ли рядом тесты (есть готовая сеть / нет / не ясно);
- `blast_radius_hint` — сколько мест/call-sites затронет починка.

Масштабируй число сканеров под размер задачи: узкий фокус — несколько линз; аудит всего проекта — полный набор + churn.

## Фаза 3 — Кластеризация и скоринг (барьер + solo)

1. **Собери все находки** (барьер: нужны все вместе) и **дедуп/кластеризуй** — один и тот же смелл, найденный разными линзами, и близкие места объединяй в один кандидат.
2. **Отскорь каждый кластер** по матрице (см. ниже): `польза × безопасность × объём × уверенность`. Приоритет — высокая **польза** при высокой **безопасности** и ограниченном **объёме**.
3. Отбери топ (обычно 5–10) для верификации; остальное пойдёт в бэклог кратко.

## Фаза 4 — Состязательная верификация топа (Workflow) + completeness-critic

1. **Верифицируй топ-кандидатов** независимыми скептиками (по умолчанию «отклонить при сомнении»). По каждому проверь три вопроса, каждый — отдельной линзой:
   - **Реален ли смелл?** Это не намеренное решение (комментарий/ADR/паттерн репо), не ложное срабатывание, доказательство подтверждается в коде.
   - **Безопасен ли рефакторинг?** Изменение behavior-preserving; внешние контракты (API/схема БД/формат сериализации/публичные сигнатуры) стабильны или blast-radius ограничен и обозрим; бизнес-логику не трогает.
   - **Настоящая ли польза?** Улучшение измеримо по заявленной оси, а не «стало красивее». Если пользу нельзя внятно назвать — кандидат слабый.
   Кандидат, проваливший «реален» или «безопасен», выбывает или падает в ранге.
2. **Completeness-critic:** отдельный агент проверяет, какой **класс смеллов не был просканирован** или какая подсистема осталась в тени. Найденное — либо добери коротким доп-сканом, либо честно отметь в отчёте как непокрытое (**не молчи о срезах**).

## Фаза 5 — Артефакт и выдача

1. Сформируй ранжированный список: **#1 — рекомендованный кандидат** (прошёл верификацию, лучший скор), далее top-N кратко.
2. Запиши артефакт `tasks/audits/AUDIT-<kebab-slug>.md` по **Шаблону** ниже: сжатый бэклог + **полная спека #1-кандидата** в формате, который прямо исполнит `refactor`.
3. В чат выдай короткое резюме: что за #1-кандидат, почему он (польза+безопасность), и next step:
   - `/prorab-tech:refactor <id>` — починить именно этого кандидата;
   - `/prorab-tech:refactor` без аргумента — авто-взять #1 из этого аудита.

---

## Каталог классов проблем (линзы для сканеров)

- **Дублирование** — copy-paste и почти-дубли логики; кандидаты на извлечение общего.
- **Сложность / размер-хотспоты** — высокая цикломатическая/когнитивная сложность, глубокая вложенность, гигантские функции/модули/классы.
- **Нарушения слоёв и связность** — бизнес-логика в контроллере/вью/шаблоне; доступ к БД из слоя API; циклические зависимости; высокая связанность (coupling↑) при низкой сцепленности (cohesion↓).
- **Мёртвый код** — недостижимые ветки, неиспользуемые экспорты/функции/поля/флаги, закомментированные блоки.
- **Обработка ошибок / надёжность** — проглоченные исключения, слишком широкие `except`/`catch`, отсутствие негативных путей, утечки ресурсов (соединения/файлы), гонки.
- **Перф-смеллы** — N+1 запросы, повторные вычисления в цикле, лишние аллокации/сериализации, отсутствие пагинации/батчинга, тяжёлые операции на горячем пути.
- **Дыры покрытия у рискового кода** — сложный/часто меняемый код без тестов (кандидат на «обвязать характеризационными тестами» — сам по себе безопасный behavior-preserving шаг).
- **Расхождение с конвенциями** — N разных способов делать одно и то же; отклонение от локального стиля/паттернов репозитория.
- **Примитивная одержимость / стрингли-типизация** — «голые» строки/словарями там, где просится тип/enum/value-object; анемичная модель.
- **Магические числа / конфиг-россыпь** — неименованные константы, разбросанные пороги/настройки.
- **Слабая типизация** — `any`/нетипизированные границы, широкие типы, потерянные инварианты (там, где язык это поддерживает).

Каталог — ориентир, а не догма: нашёл значимый класс вне списка — добавь его с доказательством.

## Матрица скоринга

Каждый кластер оценивай по четырём осям (низк./сред./выс.), итог — приоритет:

- **Польза** — насколько починка улучшает код: центральность и частота места (перекликается с churn), сила эффекта по оси (сложность↓, дублирование−, запросы↓), как сильно смелл тормозит будущую работу.
- **Безопасность** — насколько безопасно чинить: изолированность, наличие/лёгкость построения сети тестов, стабильность внешних контрактов, непричастность к бизнес-логике. **Ось-фильтр:** низкая безопасность роняет кандидата даже при высокой пользе.
- **Объём** — связный и ограниченный ли это кусок (один заход `refactor`), а не мега-переписывание.
- **Уверенность** — насколько твёрд диагноз после верификации (доказательство подтверждено, не ложное срабатывание).

**Оптимальный кандидат** = высокая польза × высокая безопасность × ограниченный объём × высокая уверенность. При равенстве — предпочитай более безопасный и более изолированный.

## Шаблон AUDIT-файла

```
# Аудит кодовой базы: <дата / фокус>

## Охват
- Что сканировали: <весь проект | фокус>
- Инструменты-сигналы: <тесты/coverage/линтеры/тайпчекер/git — что реально запускалось>
- Непокрыто (срезы): <классы/подсистемы, оставшиеся в тени, если есть>

## Ранжированный бэклог
| # | Класс | Где (file:line) | Польза | Безоп. | Объём | Увер. | Кратко |
|---|-------|-----------------|--------|--------|-------|-------|--------|
| 1 | …     | …               | выс.   | выс.   | средн.| выс.  | …      |
| 2 | …     | …               | …      | …      | …     | …     | …      |

## Рекомендованный кандидат (#1) — полная спека
### Класс проблемы
<из каталога> — <одна фраза сути>

### Где
- <file:line>, <file:line> … (все места)

### Симптом и доказательство
- <что не так> — <метрика/число/git-факт/вывод линтера, подтверждённые>

### Что улучшится (измеримо)
- Ось: <читаемость | сложность | надёжность | перф | связность | …>
- Метрика до→ожидаемое после: <напр. cyclomatic 24→<10; дублей 6→1; запросов N+1→1>

### Границы поведения (что ДОЛЖНО остаться идентичным)
- Наблюдаемое поведение / выходы: …
- Внешние контракты под риском: <API / схема БД / формат сериализации / публичные сигнатуры> — стабильны

### Статус сети тестов
- Есть готовое покрытие цели: <да/нет/частично> — <что именно>
- Что нужно дописать как характеризационную сеть ДО рефактора: …

### Blast radius
- Затрагиваемые call-sites/места: <перечень или оценка>

### Риск-спайки (проверить refactor'у ДО починки: риск → как проверить)
- …

### Почему этот кандидат (обоснование выбора)
- Польза + безопасность + объём: …
```

Разделы адаптируй/выкидывай под кандидата; для runners-up достаточно строк бэклога.

---

## Памятка по Workflow-паттернам (применяй осознанно)

- **Multi-modal sweep** — сканеры по разным линзам, каждый слеп к другим; их объединение находит то, что одна линза пропускает.
- **Барьер только на кластеризацию/скоринг** — дедуп требует всех находок сразу; сами сканеры гоняй параллельно без барьера между собой.
- **Состязательная верификация** — на каждого топ-кандидата независимые скептики с разными линзами (реален / безопасен / полезен); убивай при сомнении.
- **Completeness-critic** — финальный агент ищет непросканированный класс/подсистему.
- **Структурированный вывод** — давай агентам `schema`, чтобы возвращали валидированные находки, а не текст для парсинга.
- **Видимость** — `phase()`/`log()` для хода; масштабируй фан-аут под размер (узкий фокус — пара линз; весь проект — полный набор + churn).

## Чего НЕ делать

- Не менять код, не коммитить, не запускать ничего, что правит файлы/БД (кроме записи AUDIT-файла).
- Не рекомендовать рефакторинг, меняющий наблюдаемое поведение, выход или внешний контракт, — это продуктовое изменение (ветка `/prorab:refine` → `/prorab:build`), не тех-качество.
- Не выдавать вкусовщину за находку: нет доказательства (`file:line`/метрика/git/линтер) — нет находки.
- Не сваливать всё в «переписать всё»: итог — конкретный класс в ограниченном месте.
- Не молчать о срезах: если охват сужен или класс не просканирован — скажи об этом в отчёте.
- Не сочинять метрики: нет инструмента — оценивай вручную и честно помечай оценку как ручную.
- Не пропускать в топ кандидата, проваливший верификацию «реален»/«безопасен».
